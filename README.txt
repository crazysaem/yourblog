1.
Create the yourblog database via the yourblog_create_database.sql script on a localhost.

Make sure there is a user "root" with an empty password "" which can access the database.
Or change the credentials in yourblog/PHP/connect.php.

2.
Follwing users are currently registered on the yourblog site:

User "admin" and "Auhtor" 
with password "WebEngII"

User "User" and "Author" 
with password "passw"